node_modules/
*.tgz

# Registry publishing CLI, downloaded on demand, not source
mcp-publisher.exe
mcp-publisher
mcp-publisher.tar.gz

# DNS auth signing key for the MCP registry. Never commit.
.mcp-dns-key.txt
